torch>=2.1.0
transformers>=4.44.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
datasets>=2.19.0
vllm>=0.6.0                  # optional, for the served-model tier

scikit-learn>=1.4.0
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
pyarrow>=15.0.0
statsmodels>=0.14.0          # stratified estimation, kappa

presidio-analyzer>=2.2.0     # MIT
presidio-anonymizer>=2.2.0
spacy>=3.7.0
# Presidio needs a spaCy model at runtime and pip does not pull one:
#   python -m spacy download en_core_web_lg
# Without it AnalyzerEngine() fails at construction, not at import, so the
# absence surfaces as a stage error rather than a missing dependency.

regopy>=0.4.0                # MIT -- Rego via rego-cpp. Not OPA; DECISIONS 076.

fastapi>=0.110.0
uvicorn>=0.29.0
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0
joblib>=1.3.0

pytest>=8.0.0
pytest-cov>=5.0.0
kaggle  # optional: batch-kernel runner, scripts/kaggle_run.py
