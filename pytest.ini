[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    smoke: runs a script end to end in a subprocess; slow but the only test that exercises wiring
filterwarnings =
    error::DeprecationWarning:src.*
